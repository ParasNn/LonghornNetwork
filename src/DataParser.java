import java.io.*;
import java.util.*;

/**
 * A utility class responsible for file input operations.
 * Parses text files containing student data to populate the system.
 */
public class DataParser {
    /**
     * Reads a file and converts its content into a list of UniversityStudent
     * objects.
     *
     * @param filename The path to the file containing student data.
     * @return A list of UniversityStudent objects parsed from the file.
     * @throws IOException If the file cannot be found or read.
     */
    public static List<UniversityStudent> parseStudents(String filename) throws IOException {
        return new ArrayList<>();
    }
}
